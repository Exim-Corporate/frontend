/**
 * Theme color palette definitions - imports from shared color system
 */

import sharedColors from './shared-colors';

// Re-export the shared colors for PrimeVue theming
export default sharedColors;
